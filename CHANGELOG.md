# Changelog

このファイルは Olive Note の変更履歴です。Semantic Versioning ([https://semver.org/lang/ja/](https://semver.org/lang/ja/)) に従います。

## [1.2.12] - 2026-06-10

### 修正
- **ファビコンが表示されない問題を修正** — パッケージ版はアプリを `app/` 配下から配信する一方、ファビコンが `app/` 内にしか無く、ブラウザが要求する Web ルートの `favicon.svg` / `favicon.png` が 404 になっていた。ファビコンをパッケージのルートにも配置して表示されるようにした（あわせて巨大だった favicon.png を 128px の軽量版に作り直し）。
- **ドキュメント作成エラーの原因が分かるようにした** — これまでドキュメント作成に失敗すると固定文言「フォルダIDの設定などを確認してください」だけを表示し、サーバーが返す実際のエラー内容を握り潰していた。失敗時に実際のエラー（Google Drive API の応答）を表示し、サービスアカウントのストレージ容量や共有ドライブに関するヒントも案内するようにした。

> ℹ️ サービスアカウントは Google ドキュメント（Google ネイティブ形式）の新規作成にストレージ容量を必要とします。マイドライブ上のフォルダだと容量不足で失敗することがあります（フォルダ作成は容量を消費しないため成功します）。その場合は保管フォルダを**共有ドライブ**（要 Google Workspace）に置いてください。

## [1.2.11] - 2026-06-10

### 変更点
- **デモセットアップでも Google Drive / Vertex AI を任意設定できるように** — デモモードで無効化するのはログイン（Supabase 認証）だけになった。Google Drive と Vertex AI はデモモードでも任意入力欄として残り、入力すればファイル・ドキュメント・AI 機能も本番同様に動作する。空欄ならその機能のみ無効になる（例: Drive を空にすればファイル機能だけ無効にして、AI を含む他機能は使える）。これにより「ログインだけ簡易にして、それ以外はフルに試用してもらう」デモ構成が可能になった。

## [1.2.10] - 2026-06-10

### 新機能
- **インストーラに「デモセットアップ」モードを追加** — セットアップ時に「本番セットアップ（Supabase 認証）」か「デモセットアップ（共通パスワード）」を選べるようになった。デモを選ぶと Supabase・Google Drive・Vertex の情報入力が不要になり、共通パスワードを1つ決めるだけでセットアップが完了する。営業デモ用に、外部サービスの準備なしですぐ試用環境を立てられる。デモ環境では登録済みメールアドレスと共通パスワードでログインする（タスク・ボード・ガント等の中核機能が利用可能。ドキュメント／添付／AI は外部サービス未設定のため動作しない）。本番導入時は再セットアップで「本番セットアップ」を選ぶ。

## [1.2.9] - 2026-06-09

### 新機能
- **デモ用の簡易ログインを追加（営業デモ向け）** — Supabase / Google / Microsoft などの外部設定をせずに、登録済みメールアドレスと共通パスワードだけでログインできる「デモモード」を追加した。`config.php` で `OLIVENOTE_AUTH_PROVIDER` を `'demo'` にし、`OLIVENOTE_DEMO_PASSWORD` に共通パスワードを設定すると有効になる。ログイン画面に「デモ環境」バッジが表示される。誰がログインできるかは従来どおりメンバー管理（members）で制御し、未登録メールは弾く。本番運用では `'supabase'`（既定）に戻して使う。既定値は従来どおり Supabase Auth のままなので、既存環境の動作に影響はない。

## [1.2.8] - 2026-06-08

### 新機能
- **ホワイトボードに付箋機能を追加** — ボード上部のツールバーの「付箋を追加」から付箋を貼れるようになった。付箋にはタイトル枠・本文枠があり、枠のどこをクリックしてもその場で入力できる（本文は上揃え・左揃え）。作成者欄は OliveNote 登録メンバーからプルダウンで選べる（初期値はログインユーザー、変更可）。付箋カラーは6色プリセットから選択でき、ドラッグで移動・Excalidrawのハンドルでサイズ変更・「最前面へ／最背面へ」で重なり順を変更できる。付箋の位置・内容・色はリアルタイム同時編集で共有される。

## [1.2.7] - 2026-06-08

### 新機能
- **課題のCSV出力** — 一覧・ボード画面でフィルタリングした課題、または課題詳細を開いている個別の課題を CSV ファイルとして書き出せるようになった。一覧・ボードのツールバーの「CSV出力」ボタンは現在のフィルタ・検索結果がそのまま対象になる。課題詳細では画面下部の「CSV出力」ボタンから開いている課題を出力できる。出力項目は課題ID・課題名・ステータス・優先度・種別・カテゴリ・親課題ID・担当者/サブ担当者・各日付・実施日数・いいね数・添付ファイル名・作成/更新日時・詳細（Markdown）。コメントと画像（添付の実体）は含まず、詳細は Markdown 形式のまま出力。Excel（日本語環境）で文字化けしないよう UTF-8 BOM 付きで出力する。

## [1.2.6] - 2026-06-07

### 改善
- **AI生成ドキュメントをドキュメント一覧から除外** — タスクから AI で生成したドキュメントが、メインのドキュメント（Wiki）一覧に混在して表示されていた問題を解消。AI 生成ドキュメントは内部用として一覧から除外されるようになった。あわせて、AI 生成ドキュメントが Drive 同期時に意図せず削除対象になり得る不具合も修正。
- **ドキュメント（Wiki）同時編集でタイトルもリアルタイム同期** — これまで本文のみリアルタイム同期され、タイトルは後勝ち（保存時に上書き）だったのを、タイトルも複数人の編集が即座に反映・文字単位で自動マージされるようにした。

## [1.2.5] - 2026-06-07

### 新機能
- **ドキュメント（Wiki）のリアルタイム同時編集** — 同じドキュメントを複数人が同時に開いて編集できるようになった。本文の変更が他の参加者の画面へ即座に反映され、ヘッダーに編集中メンバーのアバターが並ぶ。さらに本文内では他メンバーのカーソル位置・選択範囲が色付きで見える。内部的には CRDT（Yjs）で競合を自動マージし、Pusher 経由でブラウザ間を直接同期する。サーバーは本文の永続化のみを担当し、同期はクライアント間 P2P で行うため軽量。
- **ホワイトボード（Pusher 同期）** — リアルタイムに共有できるホワイトボード機能を追加。

### 改善
- **横スクロール（横はみ出し）の予防ガード** — 各ビューでコンテンツが画面幅を超えて横にはみ出さないよう、予防的なレイアウトガードを追加。

## [1.2.4] - 2026-06-02

### 修正
- **ドキュメントから別ビューへ切り替えても URL が残る不具合を修正** — ドキュメント（Wiki）を開いた後にボード・一覧・ガント・カレンダー・ファイル・設定へ切り替えると URL のハッシュ（`#/wiki/...`）が残ったままになり、その状態で F5 リロードするとどの画面に居てもドキュメントに飛ばされていた問題を解消。ビュー離脱時にハッシュをリセットするようにした。
- **通知ベルの小窓・Olive コンシェルジュが外側クリックで閉じない不具合を修正** — ヘッダーの通知バッジで開く小窓と、Olive コンシェルジュのウィンドウを、ウィンドウ外をクリックすると閉じる（最小化する）ように改善。これまではもう一度ボタンを押さないと閉じられなかった。
- **エディタ（課題詳細・Wiki・コメント）で改行が詰まる不具合を修正** — 連続する空行・段落間の空けが保存・再読み込み後も保持されるようにした。
- **入力していないのに空のチェックボックスが表示される不具合を修正** — 箇条書き（`- **項目:**` など）がチェックリストに隣接している場合に、空のチェックボックスが意図せず生成されてしまう問題を解消。

### 改善
- **ドキュメント（Wiki）画面のスマートフォン対応** — モバイル表示で一覧と本文を1ペインで切り替えるレイアウトにし、本文表示中に「← 一覧」ボタンで戻れるようにした。

## [1.2.3] - 2026-06-02

### 改善
- **ドキュメント（Wiki）を開くときの待ち時間対策** — ページを開く際にローディングインジケータ（スピナー＋待機カーソル）を表示し、読み込み・表示中に画面が固まらないように改善。重い同期処理の前に一度描画を挟むことで「クリックしても無反応」を解消した。

### 修正・対策
- **画像の意図しない連続貼り付け（誤操作）への対策** — 同じ画像を短時間に連続で貼り付けた場合（Ctrl+V 連打・二度押し・同じファイルの再ドロップ等）は2枚目以降を自動スキップし、ステータスバーで通知する。中身（画像データ）で判定するため、ドラッグ&ドロップでの複数枚同時貼り付けや1枚ずつの貼り付けなど正当な操作は一切影響を受けない。あわせて1ドキュメントの画像合計容量に50MBの安全上限を設け、意図しない肥大化による読み込み時のフリーズを防止。

## [1.2.2] - 2026-06-02

### 新機能
- **エディタ内の画像サイズ調整** — 課題詳細・Wiki・コメントのリッチエディタで、貼り付けた画像をクリックすると **小 (25%) / 中 (50%) / 大 (100%) / 原寸** の幅プリセットを選べるように。画像のすぐ上に出る浮動メニュー（または上部ツールバー）から変更でき、サイズ変更後も画像の選択は保持されるので連続して微調整できる。幅は標準 Markdown（`![alt](src "50%")`）として保存され、再読み込み後も維持。PDF 出力・印刷・プレビューにも反映される

### 改善
- **画像クリックの動線を整理** — 画像クリックは「選択して幅を調整する」に統一し、Drive 上のオリジナル画像を開く操作は選択時メニューの「原寸を開く」ボタンに分離。誤解を招く虫眼鏡（zoom-in）カーソルも廃止

### 修正
- **画像を複数枚まとめて貼り付けた際に全ユーザーの画面が固まる障害を修正** — (1) Google アクセストークンのファイルキャッシュ化で毎回の OAuth 往復を排除、(2) アップロード処理中のセッションロックを早期解放、(3) Drive へのアップロード同時実行数を 2 に制限。これらにより PHP ワーカー枯渇による全体スローダウン（ページ読み込みのフリーズ）を解消

## [1.2.1] - 2026-05-29

### 改善
- **コメント欄を TipTap リッチエディタに統一** — 課題詳細・Wiki と同じ TipTap (ProseMirror) ベースのエディタをコメント入力・編集欄にも適用。太字・箇条書き・コードブロック・テーブル・画像貼り付けなど全 Markdown 機能が使えるようになった
- **コメント欄に @メンションサジェスト** — テキスト入力中に `@` を打つと候補ドロップダウンが表示され、↑↓キーまたはクリックで選択して確定できる。カーソル移動で候補外に出ると自動的に閉じる

## [1.2.0] - 2026-05-31

### 新機能
- **「ドキュメント」タブを社内 Wiki として刷新** — App ヘッダのタブを「ドキュメント (Wiki)」と「ファイル」に分離。Wiki 側は階層ツリー付きのページ管理（作成 / 編集 / 削除 / 複製 / 親子付け替え）に進化。本文は既存のリッチマークダウンエディタ（TipTap）を流用し、3 秒アイドルで自動保存、未保存のまま離脱しようとすると `beforeunload` で警告。各ページの permalink は `#/wiki/<uuid>` で固定され、移動・改題しても URL が壊れない
- **Wiki 編集履歴 & 差分ビュー** — 保存ごとに自動でリビジョンを残し、履歴ドロワーから過去版の本文・差分（jsdiff によるハイライト表示）を確認・復元できる
- **Wiki PDF 出力 & リンクコピー** — 件名 + 本文を整形した PDF を出力ボタン一発で生成（ベクター出力なので文字くっきり / 選択可）。リンクコピーは Wiki ページの permalink をワンクリックでクリップボードへ
- **旧「ドキュメント」を「ファイル」に概念リネーム** — Google Drive ミラー機能は「ファイル」タブに改名し、アイコン・見出し・空状態メッセージを刷新。DB テーブルも `documents` → `files` に rename（マイグレーション 003）。データはそのまま引き継がれ、既存リンクへの影響なし

### 改善
- **Wiki ツリーのドラッグ&ドロップを根本作り直し** — HTML5 Drag and Drop API を全廃し Pointer Events ベースの自前実装に置換。React state の lag で「たまにしか掴めない」「ドロップが効かない」現象を解消。6px 以上動かして初めてドラッグ開始（未満はクリック扱い）で、誤ドラッグでページが開かない / 動かない問題も同時に解決。マウス・タッチ共通コードで動作
- **Wiki PDF 出力エンジンを window.print 方式に置換** — html2canvas 系の「真っ白」「位置がずれる」問題を解消するため、隠し iframe + 印刷専用 CSS + `window.print()` に切替。ベクター出力で文字がくっきりし、選択 / コピーも可能に

### データベース変更（要マイグレーション）
- `003_rename_documents_to_files.sql` — `documents` テーブルを `files` にリネーム。`information_schema` で状態を確認した上で実行するので、複数回流しても安全
- `004_create_wiki_tables.sql` — `wiki_pages`（本体）と `wiki_revisions`（編集履歴）を新規作成

> 既存環境のアップグレード時は管理者画面の「🔄 システムアップデート」から自動適用される。手動運用する場合は `app/migrations/003_rename_documents_to_files.sql` → `004_create_wiki_tables.sql` の順に SQL を流す

## [1.1.1] - 2026-05-29

### 新機能
- **タスクモーダルに 3 タブ右ペインを導入** — タスク詳細画面の右側に「コメント／履歴／AI 壁打ち」の 3 タブを集約。これまで主スクロール内に縦並びで混在していたコメントと更新履歴を明確に分離し、AI チャットも同じペインに統合。タブごとに未読・件数バッジを表示
- **右ペインの幅をドラッグで調整可能に** — PC のみ。境界線をドラッグして 280〜800px の範囲で自由に幅変更でき、最後に選んだ幅はブラウザに保存され次回オープン時に復元
- **右ペインを最小化できるように** — PC のみ。デフォルトは最小化（40px 幅の縦アイコンストリップ）でメインエディタを最大限に使い、必要な時だけ展開。各アイコンには未読件数のバッジが表示され、クリックでそのタブを開きながら展開
- **モバイル向けにペイン展開ボタンを追加** — ヘッダー右の小さなチャットアイコンで全画面ドロワーとして開閉

### バグ修正
- **自分宛メンションが既読化されない不具合を修正** — 自分が書いたコメント内で自分自身をメンションした場合に、リロード後もメンションアイコンが「未読」のままになる問題を修正。コメント投稿時に投稿者を自動的に既読リストへ含めるよう変更し、既読化処理から「自分のコメント」除外フィルタを撤去

## [1.1.0] - 2026-05-29

### 新機能
- **タスクモーダルの詳細エディタを WYSIWYG 化** — 従来の `<textarea>` / Toast UI Editor を撤廃し、TipTap (ProseMirror) 2.10.3 + tiptap-markdown ベースの WYSIWYG エディタに置換。Notion 風の inline markdown shortcut（`# `→見出し、`**bold**`、`- `→箇条書き、`[ ] `→チェックリスト、`> `→引用、` ``` `→コードブロック等）が標準で動作。マークダウンとしての永続化はそのままなので既存課題の表示・編集に影響なし
- **テーブル機能を追加** — ツールバーから 3×3 テーブルを挿入可能。カーソルがテーブル内にあるとき行/列の追加・削除・ヘッダ行切替ボタンが追加表示される
- **Undo/Redo に Ctrl+Y を追加** — Ctrl+Z（取り消し）に加え Windows ユーザー向けに Ctrl+Y（やり直し）を有効化
- **コピペ時のスタイル保持** — エディタ内容を Word・メーラー等にコピペするとレンダリング済みの書式が引き継がれる
- **ツールバーを sticky 化** — モーダル内をスクロールしてもツールバーが画面上部に追従。エディタが画面外になるまで隠れない
- **詳細欄に画像を直接挿入できるように** — ツールバーの画像ボタン / クリップボード貼り付け / ドラッグ&ドロップで挿入可能。画像はブラウザ側で最大幅 1280px に自動圧縮してエディタに即時サムネイル表示し、裏で Drive に原本を保存。画像クリックで Drive 上の原本（高画質）を別タブで開ける
- **画像アップロード進捗バー** — ツールバー直下に Drive への原本保存状況（進行中／完了／失敗）を表示。失敗時は再試行ボタン付き
- **[管理者専用] Drive クリーンアップツール** — 設定画面に「Drive クリーンアップ」セクションを新設。挿入後に削除された画像など、どの課題からも参照されなくなった ATTACHMENT フォルダ内のファイルを抽出し、Drive ゴミ箱に一括移動できる（30 日以内なら復元可）。実行直前に再スキャンを走らせ、その間に再参照された ID は自動で除外する safeguard 付き

### 改善
- **AI コンシェルジュ／アドバイザーの精度・コスト改善** — タスク description に埋め込まれた base64 画像を AI 入力前に自動除去（`[画像: alt](原本URL)` に置換）。トークン浪費を防ぎつつ「画像が貼られている事実」と原本リンクは AI に伝わる

## [1.0.17] - 2026-05-28

### 修正
- **親課題プルダウンが画面下に隠れる問題を修正** — タスクモーダルの親課題プルダウンに Floating UI を導入し、画面端で開いても自動で上方向にフリップ。リスト最大高も画面余白に合わせて伸縮するようになった
- **マークダウンチェックボックスの行頭マーカーを除去** — `- [ ]` 形式のチェックボックスリストで、チェックボックスの左に表示されていた黒点（disc マーカー）を非表示に
- **ボード／一覧の分割表示で担当者フィルターが効かない問題を修正** — 分割表示を ON にしたとき「自分だけ」に暗黙絞り込みされていた挙動を撤廃し、担当者フィルターの選択値に従ってメイン担当パネル／サブ担当パネルに振り分けるよう変更。フィルターを「すべて」にすれば全員分が、特定ユーザーに絞れば対象者のメイン/サブ担当タスクがそれぞれ表示される

## [1.0.16] - 2026-05-26

### 修正
- **ガントチャートの表示崩れ修正** — 担当者フィルターのプルダウンが日付ヘッダの裏に隠れる／課題詳細モーダルを開いてもガントの課題一覧と日付ヘッダがモーダルの上に乗ってしまう問題を修正。GanttView ルートに `isolation: isolate` を付与して新しいスタッキングコンテキストを作り、内部の sticky 要素（`z-[70]`/`z-[80]`/`z-[90]`）が外側のオーバーレイ（`z-50`）に漏れ出さないようにした

## [1.0.15] - 2026-05-26

### 内部
- **認証レイヤを分離** — `index.php` / `api.php` に直書きされていた認証コード（セッション開始 / ログイン画面 HTML / Supabase JWT 検証 / logout）を `lib/auth/auth.php`（プロバイダ非依存の共通インターフェース）と `lib/auth/auth_supabase.php`（Supabase 固有実装）に分割。これにより stg（Google OAuth2 直）↔ dist（Supabase Auth）を **auth プロバイダファイルだけの差し替え** で運用できるようになり、stg → dist 同期スクリプトが `index.php`/`api.php` を上書きしても認証が壊れない構造に。`OLIVENOTE_AUTH_PROVIDER` 定数（bootstrap.php で `supabase` 固定）で provider を切替。フロントの API action 名（`verifySupabaseAuth` / `logout`）と `$_SESSION` 構造は維持しているため、エンドユーザー体験への影響はゼロ
- **Supabase ログイン画面の JS 出力に XSS 防御強化** — `json_encode` のフラグに `JSON_HEX_APOS | JSON_HEX_QUOT` を追加し、SUPABASE_URL / SUPABASE_ANON_KEY 中に万が一クォートが混入してもスクリプトブレイクアウトしないように
- **STG → dist 同期スクリプト改修** — `lib/auth/auth.php`（共通部）を同期対象に追加、provider 固有（`auth_google.php`/`auth_supabase.php`）は明示 EXCLUDE。`ListView.html` を COPY_AS_IS に登録、`.user.ini` を EXCLUDE_FROM_STG に追加（リリース時の警告解消）

## [1.0.14] - 2026-05-26

### 追加
- **ボードカードのパステル色付け** — 課題ごとに 7色（赤/橙/黄/緑/青/紫/グレー）から好きなパステル色を選んでカード全体を着色できるように。色はタスクモーダルから設定。一覧ビューでは細い色帯としてタイトル左に表示
- **ボード簡略ビュー切替** — フィルタバーの「簡略」トグルで、タイトル＋期限のみのコンパクト表示に切替可能。設定はサーバー DB に保存され、PC/別端末でも引き継がれる
- **メイン担当 / サブ担当の上下分割ビュー（PCのみ）** — ボード/一覧で「分割」トグルを ON にすると、上半分に自分がメイン担当のタスク、下半分にサブ担当のタスクが表示される。中央のリサイザーで比率調整可能。下半分はカラムヘッダーを非表示にして見やすく
- **フィルタープリセット** — よく使うフィルタ条件に名前を付けて保存。「プリセット▼」ドロップダウンからワンクリックで適用・削除可能
- **一覧ビュー** — タブに「一覧」を追加。テーブル形式でソート・列表示切替が可能。表示列の選択はユーザーごとに保存

### 改善
- **デフォルトフィルター変更** — 初期表示で全ステータスを表示するように変更。フィルタバーに「全選択 / 完了以外 / クリア」のクイックボタンを追加
- **初回ログイン時のみ「担当者=自分」を自動 ON** — 2回目以降はフィルタを保持

### 内部
- 新規 DB 列 `tasks.card_color`、新規テーブル `user_preferences` / `filter_presets` を追加（migrations/002_view_features.sql）
- API: `saveUserPreference` / `listFilterPresets` / `saveFilterPreset` / `deleteFilterPreset` の4アクションを追加

## [1.0.13] - 2026-05-26

### 改善
- **バージョン表示をヘッダー左上に移設** — 左下に固定表示していたバージョンバッジがカレンダー月ビューの日付セルと重なって日付が押せなくなることがあった問題を解消。ロゴ右隣に小さな `v1.x.y` ピル形式で常時表示するように
- **タスク詳細モーダル表示中は Olive コンシェルジュの FAB を自動的に隠す** — モーダル右下の「閉じる」ボタンと右下の FAB が重なって誤タップする問題を解消。チャットを開いている状態であれば閉じない（操作中の入力を失わない）
- **カレンダーの日付クリックモーダルがフィルターパネルの下に潜り込む問題を修正** — `main` 要素の `z-0` で発生していたスタッキングコンテキスト閉じ込めを解除し、モーダルが本来の最前面に表示されるように

## [1.0.12] - 2026-05-26

### 追加
- **課題詳細モーダルをディープリンク化** — 課題ごとに `?task=TASK-XXXX` という共有可能な URL を持たせ、直接アクセスで該当課題のモーダルが自動で開くように。モーダルヘッダの ID 横に🔗「リンクをコピー」ボタンを追加し、ワンクリックで共有用 URL をクリップボードへ。ブラウザの戻る／進むにも対応
- **description のマークダウンツールバーに「リンク」ボタンを追加** — テキストを選択した状態で押すと `[選択文字列](URL)` に変換し URL 部分を自動選択、未選択時は `[リンクテキスト](URL)` を挿入して表示文字列を自動選択。そのままペースト／入力で完成

### 改善
- **タスクモーダル開閉に「引き出し風」アニメーションを追加** — 課題を開く時に少し上から滑り降りつつ縦に伸びて、引き出しが引っ張り出されるような動きに。OSのモーション低減設定時は自動的に無効化（アクセシビリティ配慮）

## [1.0.11] - 2026-05-25

### バグ修正（テーマ切替）
- **クラシックテーマ時の AI タスクアドバイザーパネル背景が緑のままだった問題を修正** — `md:bg-olive-50` のようなレスポンシブ修飾子付きクラスにもクラシック上書きCSSを追加
- **Olive コンシェルジュの FAB ボタンがテーマに追従していなかった問題を修正** — グラデーション・ドロップシャドウを theme-aware なカスタムクラスに置換し、クラシックでは元の青／インディゴ配色に戻るように
- **その他レスポンシブ／状態修飾子（group-hover, active, focus-within 等）の取りこぼしを一括フォロー**

## [1.0.10] - 2026-05-25

### デザインリニューアル
- **カラースキームをオリーブグリーン基調にリニューアル** — 全体を blue/indigo 系から OliveNote らしい深緑 + クリームカラーへ刷新。ヘッダー・ボタン・ハイライト・AIパネル等を統一感のあるオリーブテーマに変更
- **入力フィールドの視認性を改善** — 周囲セクションを薄オリーブ背景 (`#F2F6EE`)、入力フィールドを白＋オリーブボーダー (`#9DB88A`) に統一して、コメント・項目が多いタスクでも背景と入力欄の区別が明確に。ラベルも `font-semibold` + 濃いオリーブ色で強調
- **課題詳細モーダルを画面幅いっぱいに拡大** — 従来の `max-w-5xl` (1024px) から `max-w-[96vw]` に変更。左右 2% の余白で画面から離れやすさは維持しつつ、広い画面で情報を一望できる
- **テーマ切替機能を追加** — ヘッダー右側のパレットアイコンから「オリーブ（新）」と「クラシック（旧blue/navy）」を切り替え可能。選択は `localStorage` に保存され次回起動時にも維持。将来のテーマ追加にも対応した設計

## [1.0.9] - 2026-05-22

### 機能追加 / UX 改善
- **タスクモーダルをリアルタイム自動保存に変更** — 既存タスクの「保存する」ボタンを廃止し、フィールド変更から 800ms デバウンスで自動保存。ヘッダーに「保存中… / 保存済み ✓」の保存状態インジケーターを表示
- **新規タスク作成時のトースト通知を追加** — 「作成する」ボタン押下後、登録完了が分かるようトースト表示
- **新規タスクを各ステータス列の先頭に挿入** — 従来は列の末尾に追加されていたが、即座に編集・確認しやすい列先頭に変更
- **タスクの詳細(description)欄で `@` メンションサジェストに対応** — コメント欄と同様の操作感（↑↓選択 / Tab・Enter で挿入 / Escape で閉じる）でメンバーを呼び出せる
- **メンションサジェストをカーソル位置の直下に表示** — 従来はテキストボックス左下に固定表示されていたが、入力中のカーソル真下に追従するよう改善
- **サジェスト一覧のスクロール追従** — ↑↓キーで選択を動かすと選択中の項目が必ず表示領域に入るよう自動スクロール
- **変更履歴コメント・@メンション通知の発火タイミングを統一** — モーダルを閉じる・子課題作成・複製などの「離脱」タイミングで一括発火するよう整理
- **既存タスクの下書き(draft)機能を廃止** — 自動保存に一本化し、「未保存の編集データが見つかりました」バナーの誤表示を解消

### バグ修正
- **変更履歴コメントがモーダル再オープン時に反映されない問題を修正** — フロント側 state にも履歴コメントを追記するように変更

## [1.0.8] - 2026-05-21

### バグ修正 / UX 改善
- **インストーラ Step3 の認証エラーをブラウザ画面に表示するよう修正** — JS 無効時や JS バリデーション回避時に「なぜか前画面に戻された」状態になっていた問題を解消。サーバー側のバリデーションエラーが赤いアラートで画面上に表示されるようになった
- **インストーラ Step3 のエラー時に入力値が保持されるよう修正** — Google にチェックして Client ID を入力した後 Secret を空欄で送信すると、エラーで戻ったときにチェックが外れて入力値も消えていた問題を解消。バリデーション前にセッションへ退避するよう処理順を変更
- **インストーラ Step3 に Supabase Auth Providers の説明注記を追加** — Google / Microsoft の Client ID・Secret は「Supabase の Auth Providers 設定に入れる値と同一」であることを fieldset 冒頭の注記と各 hint で明示
- **インストーラ Step3 の Client ID/Secret 欄に `required` 属性をトグル** — チェックボックス連動で `disabled` 切替と同時に `required` も同期。ブラウザネイティブのフォーム検証が機能するように
- **インストーラ Step3 の文言を修正** — 「以下の3つを設定します」を「以下の項目を設定します」に変更し、④ Vertex AI（任意）を追記

### 内部改善
- **インストーラ `handle_step2_db()` の dead code を削除** — `compact()` に同一キーの配列を `+` で結合していた冗長なコードをシンプル化
- **アップデータ画面のバージョン表示 XSS を修正** — `updater_ui.php` でマニフェスト由来の `latest` / `current` / `published_at` を `innerHTML` に未エスケープで挿入していた箇所に `escapeHtml()` を適用

## [1.0.7] - 2026-05-18

### 機能追加 / UX 改善
- **画面右下のバージョン表記を動的化**
  - 以前は固定文言「Olive Note v2」だったが、現在インストール済みのバージョンを `Olive Note vX.Y.Z` 形式で表示
  - 管理者ログイン時は manifest を自動チェックし、新版がある場合に隣に **橙色の「↻ vX.Y.Z 利用可能」ピル** を表示。クリックでシステムアップデート画面が新しいタブで開く
- **バッジ位置を左下に変更** — Olive コンシェルジュ（右下）やトースト通知（右下）と視覚的に被らないように
- **設定画面に「⚙️ システムアップデート」リンクを追加**（管理者のみ）— `app/admin/updater_ui.php` を新しいタブで開く

### 内部改善
- `index.php` 冒頭でアプリバージョンを算出 (dist は bootstrap.php の `OLIVENOTE_VERSION` 定数、STG は同階層の `VERSION` ファイル、それ以外は `'dev'`) し `window.APP_VERSION` として JS に露出
- sync スクリプトの EXCLUDE に `VERSION` を登録 (STG 固有のため dist にコピーしない)

## [1.0.6] - 2026-05-18

### 機能追加
- **AI 課題自動生成機能を新設**
  - ヘッダ右上の「🪄 AI生成」ボタンから起動
  - 画像 / PDF / Excel (.xlsx) / CSV / TSV / プレーンテキスト / 手入力メモを **複数まとめて** 入力可能（NotebookLM 風）
  - 「追加の指示」テキストで自然言語で生成方針を補足（例: 「担当は山田さん固定」「サブタスクは作らないで」）
  - **gemini-2.5-pro** が現在のカテゴリ・種別・メンバー・既存タスクを context として参照し、タスクのドラフトを一括生成
  - 親子関係（プロジェクト > サブタスク）、担当者、優先度、カテゴリ、種別、期限を AI が自動推測
  - **プレビュー画面** で各ドラフトを編集 / 除外可能。確認後に「選択した課題を保存」で一括登録
- **重複検出** を多層で実装:
  - AI が既存タスクと意味的に類似と判定したドラフトに `duplicateOfTaskId` フラグ + 根拠を付与
  - サーバ側でも件名完全一致（4文字以上）のハードルールで保険検証
  - 重複疑いドラフトは UI 上で琥珀色のバッジ + デフォルト OFF（ユーザー opt-in 必須）にしてうっかり登録を防止
- **生成品質の制御**:
  - description は必ず Markdown 形式（見出し / チェックリスト / 表 等）で生成
  - 「コンテキスト厳守ルール（ハルシネーション禁止）」をプロンプト先頭で明示。資料に書かれていないタスクや一般論補足を AI に追加させない
  - generationConfig を strict 寄り（temperature 0.1, topP 0.8）に設定

### 内部改善
- リリース sync スクリプトに `TaskAutoGenerateModal.html` を COPY_AS_IS 対象として登録
- lucide-react の Image アイコンを `ImageIcon` に rename（標準 Image コンストラクタとの混同回避）
- 新規 import: `UploadCloud`, `Check`, `Edit3`, `Table`
- SheetJS (xlsx 0.18.5) を CDN から追加（Excel クライアントサイドパース用）

## [1.0.5] - 2026-05-18

### バグ修正（重要）
- **インストーラ完了直後の `install.php?step=done` が 404 になる問題を修正**
  - 原因①: `handle_step5_finalize()` が `installer/` → `installer.locked/` にリネームしてから redirect していたため、リダイレクト先 `installer/install.php?step=done` が存在せず 404
  - 原因②: 仮にリネームしなかったとしても、`config/config.php` 存在チェックの「セットアップ済み」ガード（409）が `?step=done` を弾いて render_done に到達できなかった
  - 修正:
    1. installer のリネームを `handle_step5_finalize()` から外し、`render_done()` で HTML 出力後に `fastcgi_finish_request()` でレスポンスを確定してからリネームするように変更
    2. 「セットアップ済み」ガードに `?step=done` の例外を追加して、セットアップ完了直後の表示を許可
  - 影響範囲: v1.0.4 で migration まで完走できるようになった全環境（最終ステップが見えなかった）

## [1.0.4] - 2026-05-18

### バグ修正（重要）
- **Migration 実行時に `001_init: There is no active transaction` エラーで止まる問題を修正**
  - 原因: MySQL は `CREATE TABLE` 等の DDL を実行すると **暗黙コミット** を行うため、`beginTransaction()` で開いたトランザクションが DDL 一発目で消える。その状態で `commit()` を呼ぶと "There is no active transaction" になる
  - 修正: `commit()` を `inTransaction()` ガードで保護し、暗黙コミット済の場合は明示 commit をスキップ。`schema_migrations` への記録は DDL 後に行うように順序入れ替え
  - 影響範囲: 新規インストール時の最終ステップ（v1.0.3 で `olivenote_db()` の問題を直したことで初めて表面化）

## [1.0.3] - 2026-05-18

### バグ修正（重要）
- **インストーラ最終ステップで `TypeError: olivenote_db() must return PDO, null returned` が発生して完了できない問題を修正**
  - 原因: `install.php` の `handle_step5_finalize()` という**関数の内側**で `bootstrap.php` を `require_once` していたため、bootstrap.php 内で作られる `$pdo` 変数が関数のローカルスコープに閉じ込められ、グローバル参照する `olivenote_db()` から見えなかった
  - 修正: `bootstrap.php` で PDO 作成後に `$GLOBALS['pdo'] = $pdo;` を明示し、どのスコープから require されてもグローバル参照が必ず成立するようにした
  - 影響範囲: 新規インストールの最終ステップを実行した全ユーザー。既にセットアップ完了している環境は無関係

## [1.0.2] - 2026-05-18

### バグ修正
- **インストーラの「OAuthセットアップ手順書」「Drive準備手順書」リンクが404になっていた問題を修正**
  - 配布ZIPに `docs/` フォルダが同梱されていなかったのが原因
  - ビルドスクリプト (`scripts/build-release.sh` / `.ps1`) で `dist/docs/` を ZIP に同梱するように変更
  - markdown ファイルを綺麗にレンダリングする `docs/view.php` ビューアを追加（marked.js 使用）
  - インストーラのリンクを `view.php?doc=OAUTH_SETUP.md` 経由に変更
- アップデート時 (`app/admin/updater_ui.php`) も最新版の `docs/` を反映するよう updater を拡張

### 内部改善
- `scripts/build-release.ps1` の CHANGELOG パス解決バグを修正（`$RootDir/CHANGELOG.md` を参照するように）

## [1.0.1] - 2026-05-18

### 機能追加
- **Olive コンシェルジュに「表示中の課題」分析モード追加**
  - チャットウィンドウ上部のトグルで「💬 使い方」と「📊 表示中の課題」を切り替え可能
  - 「表示中の課題」モードはフィルター適用後の課題リスト全体を `gemini-2.5-pro` で分析
  - 件数集計・期限超過の検出・担当者ごとの負荷比較などの定量的な質問に対応
  - 「使い方」モードは従来通り `gemini-2.5-flash`（速度優先）
  - フィルター条件と現在の表示件数バッジを UI 上で可視化

## [1.0.0] - 2026-05-16

### 初回リリース
- パッケージ化対応版
- 既存のGAS版から PHP + MySQL 環境への移行が完了
- ファイル構造を `app/` `config/` `data/` `installer/` に分離
- インストーラ (`installer/install.php`) で対話的セットアップ
- アップデーター（管理者画面のボタンから自動更新）
- マイグレーション機構を導入し DB スキーマを自動適用
- スマホレスポンシブ対応（全7ビュー）

### 機能一覧
- ボード / ガントチャート / カレンダー / ドキュメント / 設定 の5ビュー
- Markdown でリッチな課題詳細
- 親子課題（2階層）
- 添付ファイル (Google Drive 連携)
- AI コンシェルジュ / AI タスクアドバイザー (Vertex AI)
- AI でドキュメント自動生成
- 通知 / コメント / メンション
- リリースノート自動生成（AI）
